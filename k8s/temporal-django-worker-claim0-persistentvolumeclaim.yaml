apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: temporal-django-worker-claim0
  name: temporal-django-worker-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
