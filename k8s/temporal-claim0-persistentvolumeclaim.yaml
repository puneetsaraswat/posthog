apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: temporal-claim0
  name: temporal-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
