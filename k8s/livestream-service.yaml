apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: ./kompose -f docker-compose.hobby.yml convert
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: livestream
  name: livestream
spec:
  ports:
    - name: "8666"
      port: 8666
      targetPort: 8080
  selector:
    io.kompose.service: livestream
