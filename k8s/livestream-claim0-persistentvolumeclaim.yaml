apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: livestream-claim0
  name: livestream-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
