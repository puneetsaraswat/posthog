apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: clickhouse-claim5
  name: clickhouse-claim5
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
